{% extends 'base.html.twig' %}

{% block title %}Coche{% endblock %}

{% block body %}
    <h1>Coche</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coche.id }}</td>
            </tr>
            <tr>
                <th>Marca</th>
                <td>{{ coche.marca }}</td>
            </tr>
            <tr>
                <th>Modelo</th>
                <td>{{ coche.modelo }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ coche.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('coche_index') }}">back to list</a>

    <a href="{{ path('coche_edit', {'id': coche.id}) }}">edit</a>

    {{ include('coche/_delete_form.html.twig') }}
{% endblock %}
